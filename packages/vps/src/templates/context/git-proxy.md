## Git
Git push requires authorization via the gateway.
Local git operations (add, commit, branch, log, etc.) work freely.
NEVER configure git credentials manually (no SSH keys, no tokens). The dashboard handles everything.