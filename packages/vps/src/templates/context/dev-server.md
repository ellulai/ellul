## Dev Server (AUTOMATIC)
Fully managed. The platform auto-detects your framework, installs dependencies, and starts the dev server on port __PREVIEW_PORT__.
DO NOT run pm2, npm run dev, or any process/dependency management commands. Just write code.
Preview URL: https://__DEV_DOMAIN__