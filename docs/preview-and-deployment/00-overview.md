# Preview and deployment

How an app moves from "running locally" to "viewable on a `*.ellul.app` preview URL" to "deployed in production." Covers the dev-server runtime, framework detection, origin-rewriting, monorepo handling, and the production deployment pipeline.

## Section index

| File | Purpose |
| --- | --- |
| `01-preview-runtime.md` | Dev-server runtime, framework probe, port allocation, Origin rewriting, lifecycle. |
| `02-deployment.md` | Production deployment flow, build artefacts, hosting model. |
| `03-monorepo.md` | Monorepo handling: app discovery, dependency resolution, per-app builds. |

Migrated from legacy `docs/PREVIEW.md` and `docs/PREVIEW-AND-DEPLOYMENT.md`.
